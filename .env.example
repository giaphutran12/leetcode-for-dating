# Server-side OpenAI key for the LLM judge. Read only at runtime by the Vite judge
# plugin (server-side); never exposed to the browser. Copy to .env.local and fill in.
OPENAI_API_KEY=

# Optional: override the judge model. Defaults to gpt-5.4 when unset.
RIZZCODE_JUDGE_MODEL=
